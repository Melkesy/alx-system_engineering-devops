#0x00.shell, basics
0-current_working_directory -write a script that prints the absolute path name of the current working directory.
1-listit - Display the content list of your current directory.
2-bring_me_home -write a scripts that changes the working directory to the user's home directory.
3-listfiles -Display current directory contents in a long format
4-listmorefiles -Display current directory contents. including hidden files (starting with .).
5-listfilesdigitonly -Display current directory contents.long format with user and group IDs displayed numerically and hidden files (starting with .).
6-firstdirectory -A script that creates a directory named my_first_directory.
7-movethatfile -Move the file betty from /tmp/ to /tmp/my_first_directory.
8-firstdelete -Delete the file betty.the file betty is in /tmp/my_first_directory.
9-firstdirdeletion -Delete the directory my_first_directory that is in the /tmp directory.
10-back -A scripts that changes the working directory to the previous one.
11-lists -A script that lists all files in longer format.
12-file_type -A script that prints the type of the file named iamfile.
13-symbolic_link -A symbolic link to /bin/ls, named ls.
14-copy_html -A scripts that copies all the HTML files from the current working directory to the parent of the working directory, but only copy files that did not exist in the parent of the working directory or were newer than the version in the parent of the working directory.
